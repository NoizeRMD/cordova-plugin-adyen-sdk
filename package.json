{
  "scripts": {
    "install-plugin": "cd demo && rm -rf platforms plugins && npm i && cordova platform add ios android && cordova plugin add ../source --force && cd ..",
    "ios": "cd demo && cordova run ios",
    "android": "cd demo && cordova run android"
  }
}
